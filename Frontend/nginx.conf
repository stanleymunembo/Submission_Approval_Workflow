# Simple web server rules for React Router pages.
# Without this, refreshing /login might show "404 Not Found".

server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
